apiVersion: v1
kind: Service
metadata:
  name: {{ include "historian-helm.fullname" . }}
  labels:
    {{- include "historian-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "historian-helm.selectorLabels" . | nindent 4 }}
